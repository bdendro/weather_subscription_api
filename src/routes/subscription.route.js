import { Router } from 'express';

const subscriptionRouter = Router();

// subscriptionRouter.post('/subscribe')
// subscriptionRouter.get('/confirm/:token')
// subscriptionRouter.get('/unsubscribe/:token');

export default subscriptionRouter;
